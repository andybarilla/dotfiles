# Login shell entry point. Most config lives in ~/.bashrc.
[ -f ~/.bash_env ] && . ~/.bash_env
[ -f ~/.bashrc ] && . ~/.bashrc
[ -f ~/.cargo/env ] && ~/.cargo/env


# Added by Toolbox App
export PATH="$PATH:/home/andy/.local/share/JetBrains/Toolbox/scripts"

